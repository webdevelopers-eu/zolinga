---
name: example-skill
description: "Use when: working with this module. Replace this description with specific trigger phrases describing what the skill helps accomplish and when to invoke it."
argument-hint: "[topic or goal]"
---

# Example Skill

> Replace this file with a real skill for your module.
> Rename the `skills/example-skill/` directory to match your skill's purpose (for example, `skills/my-module-deploy/`).
> The installer auto-symlinks each skill directory into `.agents/skills/{module}-{skill-name}` on every install/update run.

## Use When

- Describe the specific task or workflow this skill covers.
- Be concrete: the description field above is what the agent uses to decide whether to load this skill.

## Workflow

1. Step one.
2. Step two.
3. Step three.

## References

- `system/wiki/Zolinga Core/Module Anatomy.md` — module folder structure including skills directory.
- `.agents/README.md` — agent skills system overview.
- `system/wiki/Zolinga Core/Module Installation and Updates.md` — Agent Skills auto-symlink behavior.
